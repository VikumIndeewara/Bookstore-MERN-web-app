{
  "name": "mern-bookstore",
  "description": "A MERN stack bookstore web application",
  "main": "server.js",
  "scripts": {
    "start":"node index.js",
    "dev" : "nodemon index.js",
    "build": "npm install && npm install --prefix Frontend && npm run build --prefix Frontend"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "mongoose": "^8.4.0",
    "react-icons": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
